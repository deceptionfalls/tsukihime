const header = document.querySelector('h3'); // selects the h3
const list = document.querySelector('ul'); // selects the ul

if (element.matches(':hover')){
  header.classList.toggle('toggled') // changes color of the h3's background.
}
