# file manager
super + e 
	pcmanfm

# firefox
super + w
	firefox

# print screen
super + q
  flameshot gui

# lock screen
super + shift + q
	betterlockscreen -l

# checker for caps lock
Caps_Lock
	/home/tsukki/.config/dunst/dunst_caps_lock.sh

# rofi or dmenu
super + shift + Return
	rofi -show drun

#
# mpd
#

# next
super + shift + p
    mpc --host localhost --port 8800 next

# prev
super + shift + o
    mpc --host localhost --port 8800 prev

# toggle
super + shift + i
    mpc --host localhost --port 8800 toggle
